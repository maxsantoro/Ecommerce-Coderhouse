import { createContext } from "react";

export const userContext = createContext();

const UserContext = () => {
    return (
        <div>
            
        </div>
    );
}

export default UserContext;
